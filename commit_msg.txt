feat: Add search, language switcher, and dark/light mode to navbar

- Command palette search (Ctrl+K) covering all pages, sessions, themes
  with keyboard navigation (arrow keys, enter, escape)
- Language switcher supporting English, French, Portuguese, ChiShona
  with flag icons and translated nav labels + CTA text
- Dark / Light / System theme toggle via next-themes
- Theme-aware CSS variables in globals.css (light mode overrides for
  body, glass, hero, section backgrounds)
- Navbar fully adapts to active theme (text, border, bg colors)
- Mobile sidebar includes language grid and appearance picker
